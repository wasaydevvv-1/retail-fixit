# API base URL (proxied through Vite in dev)
VITE_API_BASE_URL=/api/v1

# Entra ID (leave empty for dev login picker)
VITE_AZURE_AD_TENANT_ID=
VITE_AZURE_AD_CLIENT_ID=
VITE_AZURE_AD_API_SCOPE=

# Realtime endpoint (local WebSocket in dev, Azure SignalR negotiate URL in prod)
VITE_REALTIME_URL=/ws